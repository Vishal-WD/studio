// This file is intentionally left blank.
// It's required for Firebase Cloud Messaging to work in the background.
// Firebase will automatically handle the service worker registration.
self.addEventListener('push', (event) => {
    // Optional: You can add custom background push handling here.
    // For now, we'll let Firebase handle displaying the notification.
});
